apiVersion: v1
kind: Service
metadata:
  name: floci
  namespace: floci
spec:
  selector:
    app: floci
  ports:
    - port: 4566
      targetPort: 4566
---
apiVersion: v1
kind: Service
metadata:
  name: floci-dash
  namespace: floci
spec:
  selector:
    app: floci-dash
  ports:
    - port: 3000
      targetPort: 3000
